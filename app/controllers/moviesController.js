// Import Model

// Import Helpers
const { registerValidation, loginValidation } = require('../helpers/Validation')
const response = require('../helpers/Response')

exports.createMovie = async (req, res) => {
  return res.send('create movie')
}
